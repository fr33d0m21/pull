import { TrackingEntry } from '../types';

export function parseTrackingFile(fileContent: string): TrackingEntry[] {
    console.error("You are calling the tracking parser, but you shouldnt be!")
    return [];
}